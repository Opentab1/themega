#!/bin/bash

echo "???????????????????????????????"
echo "?? PULSE BAR AI ? QUICK FIX"
echo "???????????????????????????????"

# Stop any running processes
echo "?? Stopping running services..."
pkill -f "python.*app.py" 2>/dev/null
pkill -f "vite" 2>/dev/null
sleep 2

# Pull latest changes
echo "?? Pulling latest fixes from repository..."
git pull origin cursor/fix-pulse-bar-ai-installation-and-sensor-data-b590

# Make sure we're in the right directory
cd ~/pulse-bar-ai 2>/dev/null || cd "$(dirname "$0")"

# Restart everything
echo "?? Starting fixed version..."
./run.sh
